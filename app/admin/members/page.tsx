import { Download, Search } from "lucide-react";
import { members } from "../../../lib/data";

export default function AdminMembersPage() {
  return <><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-lime">Manage</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Members.</h1></div><button className="inline-flex w-fit items-center gap-2 rounded-xl border border-line px-4 py-3 text-sm font-semibold text-white hover:border-white"><Download size={16} /> Export CSV</button></div><div className="mb-5 flex gap-3"><div className="relative flex-1"><Search className="absolute left-4 top-3 text-slate-500" size={16} /><input placeholder="Search members..." className="w-full rounded-xl border border-line bg-panel py-3 pl-11 text-sm text-white outline-none focus:border-lime" /></div></div><div className="overflow-hidden rounded-2xl border border-line bg-panel">{members.concat([{ name: "Linh Vo", role: "Growth lead", initials: "LV", color: "bg-sky-300 text-ink", joined: "Jul 2025", status: "Active" }]).map((member) => <div key={member.name} className="flex items-center gap-4 border-b border-line px-5 py-4 last:border-0 sm:px-6"><span className={`flex h-9 w-9 items-center justify-center rounded-full text-xs font-bold ${member.color}`}>{member.initials}</span><div className="min-w-0 flex-1"><p className="text-sm font-semibold text-white">{member.name}</p><p className="text-xs text-slate-500">{member.role}</p></div><span className="hidden text-xs text-slate-500 sm:block">{member.joined}</span><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${member.status === "Active" ? "bg-lime/10 text-lime" : "bg-orange-300/10 text-orange-300"}`}>{member.status}</span><button className="text-xs text-slate-500">View</button></div>)}</div></>;
}
