import { ArrowUpRight, MoreHorizontal, TrendingUp } from "lucide-react";
import { stats, events } from "../../lib/data";
import { StatCard } from "../../components/stat-card";

export default function AdminPage() {
  return <><div className="mb-10 flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-lime">Admin console</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Club overview.</h1></div><span className="hidden items-center gap-2 text-xs text-slate-500 sm:flex"><span className="h-2 w-2 rounded-full bg-lime" /> All systems operational</span></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{stats.map((stat) => <StatCard key={stat.label} {...stat} />)}</div><div className="mt-8 grid gap-5 lg:grid-cols-[1.5fr_1fr]"><div className="rounded-2xl border border-line bg-panel p-6"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Member growth</h2><p className="mt-1 text-xs text-slate-500">Last 6 months</p></div><TrendingUp className="text-lime" size={19} /></div><div className="mt-8 flex h-48 items-end gap-3 border-b border-line px-2">{[35, 48, 42, 64, 72, 91, 82, 100, 94, 100, 110, 124].map((height, i) => <div key={i} className="group relative flex-1"><div className="absolute -top-5 hidden w-full text-center text-[9px] text-slate-500 group-hover:block">{height}</div><div style={{ height: `${height}%` }} className={`w-full rounded-t-sm ${i === 11 ? "bg-lime" : "bg-lime/20"}`} /></div>)}</div><div className="mt-3 flex justify-between text-[10px] text-slate-600"><span>Mar</span><span>Apr</span><span>May</span><span>Jun</span><span>Jul</span><span>Aug</span></div></div><div className="rounded-2xl border border-line bg-panel p-6"><div className="flex items-center justify-between"><h2 className="font-semibold">Upcoming events</h2><ArrowUpRight size={16} className="text-slate-600" /></div><div className="mt-5 space-y-4">{events.slice(0, 3).map((event) => <div className="flex gap-3" key={event.slug}><div className="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg bg-lime/10 text-xs font-bold text-lime">{event.date.split(" ")[1].replace(",", "")}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium text-white">{event.title}</p><p className="mt-1 text-[11px] text-slate-500">{event.attendees} attendees · {event.location}</p></div><MoreHorizontal size={15} className="text-slate-600" /></div>)}</div></div></div></>;
}
