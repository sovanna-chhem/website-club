import { Plus, Search } from "lucide-react";
import { events } from "../../../lib/data";

export default function AdminEventsPage() {
  return <><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-lime">Manage</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Events.</h1></div><button className="inline-flex w-fit items-center gap-2 rounded-xl bg-lime px-4 py-3 text-sm font-bold text-ink"><Plus size={16} /> New event</button></div><div className="mb-5 flex gap-3"><div className="relative flex-1"><Search className="absolute left-4 top-3 text-slate-500" size={16} /><input placeholder="Search events..." className="w-full rounded-xl border border-line bg-panel py-3 pl-11 text-sm text-white outline-none focus:border-lime" /></div><select className="rounded-xl border border-line bg-panel px-4 text-sm text-slate-400"><option>All status</option><option>Published</option><option>Draft</option></select></div><div className="overflow-hidden rounded-2xl border border-line bg-panel">{events.map((event) => <div key={event.slug} className="flex flex-col gap-4 border-b border-line px-5 py-5 last:border-0 sm:flex-row sm:items-center sm:px-6"><div className="flex h-12 w-12 shrink-0 flex-col items-center justify-center rounded-lg bg-lime text-ink"><span className="text-[9px] font-bold">{event.date.split(" ")[0]}</span><span className="text-lg font-bold">{event.date.split(" ")[1].replace(",", "")}</span></div><div className="min-w-0 flex-1"><h2 className="font-semibold text-white">{event.title}</h2><p className="mt-1 text-xs text-slate-500">{event.location} · {event.attendees}/{event.capacity} registered</p></div><span className="w-fit rounded-full bg-lime/10 px-2 py-1 text-[10px] font-bold text-lime">Published</span><button className="text-xs text-slate-500 hover:text-white">Edit</button></div>)}</div></>;
}
