import Link from "next/link";
import { CalendarDays, ChevronRight } from "lucide-react";
import { events } from "../../../lib/data";

export default function MyEventsPage() {
  return <><div className="mb-10"><p className="text-xs font-bold uppercase tracking-[.2em] text-lime">Your calendar</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">My events.</h1><p className="mt-2 text-sm text-slate-400">Keep track of the rooms you&apos;re joining.</p></div><div className="space-y-3">{events.slice(0, 3).map((event, i) => <Link href={`/events/${event.slug}`} key={event.slug} className="flex flex-col gap-4 rounded-2xl border border-line bg-panel p-5 transition hover:border-slate-500 sm:flex-row sm:items-center"><div className="flex h-16 w-16 shrink-0 flex-col items-center justify-center rounded-xl bg-lime text-ink"><span className="text-[10px] font-bold uppercase">{event.date.split(" ")[0]}</span><span className="text-2xl font-bold">{event.date.split(" ")[1].replace(",", "")}</span></div><div className="flex-1"><span className="text-[10px] font-bold uppercase tracking-wider text-lime">{i === 0 ? "Attending" : "Upcoming"}</span><h2 className="mt-1 font-semibold text-white">{event.title}</h2><p className="mt-1 text-xs text-slate-500">{event.date} · {event.location}</p></div><ChevronRight className="text-slate-600" size={18} /></Link>)}</div><div className="mt-8 rounded-2xl border border-dashed border-line p-8 text-center"><CalendarDays className="mx-auto text-slate-600" /><p className="mt-3 text-sm text-slate-400">Looking for something new?</p><Link href="/events" className="mt-2 inline-block text-sm font-semibold text-lime">Explore the calendar →</Link></div></>;
}
