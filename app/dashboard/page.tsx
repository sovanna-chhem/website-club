import Link from "next/link";
import { ArrowUpRight, CalendarDays, ChevronRight, Clock3 } from "lucide-react";
import { EventCard } from "../../components/event-card";
import { events, resources } from "../../lib/data";

export default function DashboardPage() {
  return <><div className="mb-10 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-lime">Tuesday, August 25</p><h1 className="mt-3 text-3xl font-semibold tracking-tight md:text-4xl">Good morning, Minh<span className="text-lime">.</span></h1><p className="mt-2 text-sm text-slate-400">Here&apos;s what&apos;s happening in your club.</p></div><Link href="/events" className="inline-flex items-center gap-2 text-sm font-semibold text-lime">Browse events <ArrowUpRight size={15} /></Link></div><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-line bg-panel p-5 sm:col-span-2"><div className="flex items-center justify-between"><div><p className="text-xs text-slate-500">Your next event</p><h2 className="mt-2 text-xl font-semibold text-white">{events[0].title}</h2></div><span className="rounded-full bg-lime/10 px-3 py-1 text-xs font-semibold text-lime">In 4 days</span></div><div className="mt-8 flex flex-wrap gap-5 text-xs text-slate-400"><span className="flex items-center gap-2"><CalendarDays size={14} className="text-lime" />{events[0].date}</span><span className="flex items-center gap-2"><Clock3 size={14} className="text-lime" />{events[0].time}</span><button className="ml-auto text-xs font-semibold text-white hover:text-lime">View details <ChevronRight className="inline" size={14} /></button></div></div><div className="rounded-2xl border border-line bg-lime p-5 text-ink"><p className="text-xs font-bold uppercase tracking-wider">Your impact</p><p className="mt-6 text-4xl font-semibold">12</p><p className="mt-1 text-sm">connections made</p><p className="mt-5 text-xs opacity-70">Top 8% of members this month</p></div></div><div className="mt-14"><div className="mb-6 flex items-center justify-between"><h2 className="text-lg font-semibold text-white">Coming up</h2><Link href="/events" className="text-xs text-slate-500 hover:text-white">See calendar →</Link></div><div className="grid gap-4 md:grid-cols-2">{events.slice(1, 3).map((event) => <EventCard key={event.slug} event={event} />)}</div></div><div className="mt-14"><div className="mb-6 flex items-center justify-between"><h2 className="text-lg font-semibold text-white">From the library</h2><Link href="/dashboard/resources" className="text-xs text-slate-500 hover:text-white">View all →</Link></div><div className="grid gap-4 md:grid-cols-3">{resources.map((resource) => <div key={resource.title} className="rounded-2xl border border-line bg-panel p-5"><span className="text-2xl text-lime">{resource.icon}</span><p className="mt-7 text-[10px] font-bold uppercase tracking-wider text-slate-500">{resource.type}</p><h3 className="mt-2 font-semibold text-white">{resource.title}</h3><p className="mt-2 text-xs leading-5 text-slate-400">{resource.description}</p></div>)}</div></div></>;
}
