import { ArrowUpRight, BookOpen } from "lucide-react";
import { resources } from "../../../lib/data";

export default function ResourcesPage() {
  return <><div className="mb-10"><p className="text-xs font-bold uppercase tracking-[.2em] text-lime">The library</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Useful things, collected.</h1><p className="mt-2 text-sm text-slate-400">Playbooks, templates, and ideas from the community.</p></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{resources.concat([{ title: "How to host a great room", type: "Guide", description: "A field guide to designing gatherings that people remember.", meta: "8 min read", icon: "◌" }]).map((resource) => <article key={resource.title} className="group rounded-2xl border border-line bg-panel p-6 transition hover:-translate-y-1 hover:border-slate-500"><div className="flex items-center justify-between"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-lime/10 text-xl text-lime"><BookOpen size={19} /></span><ArrowUpRight size={18} className="text-slate-600 group-hover:text-lime" /></div><p className="mt-10 text-[10px] font-bold uppercase tracking-wider text-lime">{resource.type}</p><h2 className="mt-2 text-xl font-semibold text-white">{resource.title}</h2><p className="mt-3 text-sm leading-6 text-slate-400">{resource.description}</p><p className="mt-7 text-xs text-slate-600">{resource.meta}</p></article>)}</div></>;
}
